import React, { useState } from "react";
import BarGraph from "./BarGraph";
const Dashboard = () => {
  const [bar, setBar] = useState({});

  const loadProjectDeatails = () => {};

  return <></>;
};

export default Dashboard;
